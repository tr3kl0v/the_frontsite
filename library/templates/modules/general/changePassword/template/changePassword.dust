<article class="formWrapper" id="changePassword">
	<h2>{title}</h2>
	
	<form id="articleSearchForm" action="/users/password" method="post">
		{#fields}
			{>textField /}			
			{>passwordField /}
		{/fields}

		<p><span class="required">*</span>{requiredFieldsLabel}</p>

		{>submitButton /}
	</form>
</article>